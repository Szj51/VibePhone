# VibePhone · 可用版本 v1.0

**固化日期**：2026-09-21
**状态**：✅ **已通过端到端稳定性验证，功能完整可用**
**验证结果**：串口触发 31 次，成功 31 次，失败 0 次，崩溃 0 次

> ⚠️ **这是第一个完整可用的版本。改动前请先备份，或参考本文件底部的恢复方法。**

> 📁 **目录名对照**：本文写于原始备份包，其中 `2.esp32s3usbHID/` 即本仓库的 `firmware/`，`1.1.按键打开.claude/` 即 `bridge/`。文件内容未改动。
>
> 🔤 **换行符说明**：本仓库按 `.gitattributes` 统一以 **LF** 存储。原备份包中 `platformio.ini` 为 CRLF，因此它在仓库里的 SHA256 与下方指纹不同；其余文件原本就是 LF，指纹一一对应。

---

## 这一版能做什么

| 功能 | 状态 |
| --- | --- |
| ESP32-S3 物理按键 → 打开 Codex / Claude Code | ✅ |
| 按键同时作为标准 USB 键盘发 F13 / F14 | ✅ |
| 「键盘 + 串口」复合设备（一个 USB 口两个功能） | ✅ |
| 串口端口自动发现 + 断线重连 | ✅ |
| 网页控制台按键（`1` / `2`） | ✅ |
| 屏幕显示 | ❌ **本版关闭**（见「已知限制」） |

---

## 包含的三个组件

### 1. 固件（`firmware/`）

| 项 | 值 |
| --- | --- |
| 平台 | PlatformIO + `espressif32@7.1.3` + Arduino core 4.x |
| 目标板 | `esp32-s3-devkitc-1`（优信 YD-ESP32-23 V1.3，N16R8） |
| 按键 | GPIO4 → F13（`0x68`）、GPIO5 → F14（`0x69`） |
| `TFT_ENABLE` | **`0`**（屏幕代码关闭） |
| `HEARTBEAT_MS` | `3000` |
| 烧录波特率 | `115200` |
| Flash 分区 | 16MB 布局（app 6.25MB ×2，SPIFFS 3.38MB） |

### 2. 桥接程序（`bridge/`）

| 项 | 值 |
| --- | --- |
| 版本 | 0.2.0 |
| 监听端口 | `127.0.0.1:17653`（仅回环，不经过防火墙） |
| 依赖 | **零第三方 npm 依赖** |

### 3. 配置（`bridge/config.json`）

```json
"keys":   { "codex": "F13", "claude": "F14" },
"device": { "enabled": true, "port": "", "buttons": ["codex", "claude"] }
```

---

## 源文件指纹（SHA256 前 16 位）

比对指纹可以确认文件有没有被改动过：

```
3204a9126e013f10  bridge.mjs
46a47147a2d1ca45  launcher.mjs
2c8108d91c0bdadd  config.json
618bb75e97a3e7af  platform/launch-windows.ps1
2a2e8ee265e95338  platform/serial-windows.ps1
c261ffbda007f89b  src/main.cpp
468742e879273025  src/display.cpp
246e6acb8acbef5a  include/config.h
b35e68fbf22d743f  include/display.h
e3660727e2006db3  platformio.ini
```

**注意**：两个 `.ps1` 必须保持**带 UTF-8 BOM**（用编辑器另存时别弄丢，否则脚本会报几十处语法错误）。

---

## 本版修掉的问题（都写进了各自的 README）

| # | 问题 | 症状 |
| --- | --- | --- |
| 1 | `ARDUINO_USB_MODE` 被板级定义强制覆盖 | USB HID 根本不工作 |
| 2 | 16MB Flash 只用到一半 | 分区表按 8MB 生成 |
| 3 | `press()` 把键码当 ASCII | 按 F13 打出字母 `h` |
| 4 | `USBCDC::write` 无超时忙等 | 缓冲区满时**整个固件卡死** |
| 5 | `.ps1` 缺 BOM | 34 处语法错误，启动器不工作 |
| 6 | 端口发现被 ROM 调试文字骗到 | "显示已连接但按键无反应" |
| 7 | Codex 菜单项固定等 700ms | 间歇性误报"没有菜单项" |
| 8 | **`COM` 口被程序打开会把芯片复位进下载模式** | **应用停、按键全哑** |

---

## 已知限制与使用规则

### ⚠️ 规则一：`COM`（FTDI）线日常必须拔掉

那块板把 FTDI 的 `DTR` 接在了芯片的 `GPIO0`（BOOT 脚）上。**任何程序打开 `COM` 口都可能把芯片复位进下载模式**，而且会一直卡住。

| 场景 | `COM` 线 |
| --- | --- |
| 日常使用 | ❌ 拔掉 |
| 烧录固件 | ✅ 插上 → 烧完 → 拔掉 |

**故障现象**：设备管理器里只剩串口、**没有键盘**，按键全哑。
**恢复**：拔掉 `COM` 线 → 按 `RST` → 重启桥接程序。

### ⚠️ 规则二：烧录前先关掉桥接程序

否则可能在写完 bootloader 后报「芯片停止响应」。

### 其他限制

- **屏幕未启用**：`TFT_ENABLE = 0`。屏幕驱动代码已写好但未调通（接线后只有背光、无画面），需要时再单独排查。
- **macOS 未真机验证**：代码已提供，但只在 Windows 上跑过。
- **本机开发环境在项目外**：`C:\Users\szj\.platformio`（约 1.7GB 工具链）。这是 PlatformIO 装的，不是本项目产生的。收件人重新编译需要装 PlatformIO。

### 系统足迹（符合"不打开就等于没配置过"）

```
✅ 不写注册表          ✅ 不装服务
✅ 不加开机自启        ✅ 不开防火墙端口（只监听 127.0.0.1）
✅ 项目外无文件        ✅ 关掉就全停，串口释放
```

---

## 恢复方法

如果这一版被改坏了，想回到这个状态：

1. **源码**：解压 `VibePhone-可用版本-v1.0-20260921.zip`，覆盖两个文件夹
2. **固件**：重新编译烧录（`pio run -t upload --upload-port COM7`）
3. **确认**：比对上面的 SHA256 指纹

> 原始备份包**包含构建产物**（`.pio/`，37MB），所以不需要重新下载依赖就能直接烧录。本仓库为保持可读性**不包含** `.pio/`，克隆后需自行 `pio run` 编译。
